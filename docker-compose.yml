services:
  tosub2:
    build: .
    # 服务器部署可直接用 CI 构建好的镜像（GitHub Actions 自动推送到 GHCR）：
    # image: ghcr.io/zhoudashuaibi/tosubv2:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:1999:1999"       # 只绑本机，由反代转发；确需直连再改 0.0.0.0
    volumes:
      - ./data:/app/data
    environment:
      TOSUB2_CONSOLE_PASSWORD: ${TOSUB2_CONSOLE_PASSWORD:-}   # 首次种子，可留空走首访引导
      TOSUB2_SECRET_KEY: ${TOSUB2_SECRET_KEY:-}               # 建议显式提供
      TOSUB2_PORT: "1999"
    mem_limit: 1g
