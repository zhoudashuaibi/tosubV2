# 01 总体架构

## 1. 系统目标

toSub2 v2 是一套部署在公网（HTTPS 反代之后）的 ChatGPT 账号池管理系统：

1. **代理列表**：批量导入代理，一键测活；所有需要请求 OpenAI 接口的操作（登录、刷余额等）随机选用存活代理，无可用代理时回退本机直连。
2. **三级号池**：
   - **备用号池**：导入 `邮箱----密码----clientId----refreshToken` 格式的 Outlook 账号，从邮件列表初始化邮箱名、初始余额、封禁状态；支持单选/批量加入主号池。
   - **主号池**：账号加入时执行邮箱验证码登录（复用现有核心协议），登录后通过接口获取实时余额；支持批量授权、批量上传 sub2api、批量获取余额。
   - **废弃号池**：sub2api 侧出现 401 封禁或 429 限流的账号自动移入（记录原因），也支持手动废弃与移回。
3. **sub2api 管理**：配置后端地址与管理员密钥；上传时可选禁用 5h/7d 自动暂停、并发数、优先级、负载因子、模型白名单、自动分配 sub2api 内已有代理；sub2api 已存在的账号替换凭据、不存在的新增。
4. **可用性**：页面密码保护但刷新不要求重输（30 天会话）；数据全量持久化，重启不丢；左侧菜单 + 右侧内容的经典后台布局。

### 非目标（v2 不做）

- 多用户/角色权限体系（单管理员密码）。
- 不改写 OpenAI 登录协议本身（协议层整体复用 v1）。
- 不做移动端专门适配（响应式可用即可）。

## 2. 总体架构图

```
                         ┌──────────────────────────────────────────────┐
                         │              浏览器 (React SPA)               │
                         │  shadcn-admin 布局：左侧菜单 + 右侧内容页        │
                         │  TanStack Router / TanStack Query (轮询)       │
                         └───────────────────┬──────────────────────────┘
                                             │ HTTPS (同源, HttpOnly Cookie 会话)
                                    ┌────────▼─────────┐
                                    │  Nginx / Caddy    │  TLS 终端（可选直连）
                                    └────────┬─────────┘
                                             │ HTTP
┌────────────────────────────────────────────▼─────────────────────────────────────┐
│                            toSub2 容器（单镜像同源）                                │
│                                                                                    │
│  ┌────────────────────────── Fastify HTTP 服务 (Node 22) ──────────────────────┐  │
│  │  /api/v1/auth      认证与会话（登录/登出/限流）                                │  │
│  │  /api/v1/proxies   代理列表（导入/测活/管理）                                  │  │
│  │  /api/v1/accounts  三级号池（导入/加池/批量操作/废弃/恢复）                      │  │
│  │  /api/v1/jobs      任务中心（列表/日志/人工输入/取消/重试）                       │  │
│  │  /api/v1/sub2api   sub2api 配置/上传/监控                                     │  │
│  │  /api/v1/settings  系统设置（密码/会话/Outlook取件/接码平台/并发）               │  │
│  │  /api/v1/dashboard 概览统计          /api/v1/health 健康检查                  │  │
│  │  其余路径 → 静态托管前端 dist + SPA fallback                                  │  │
│  └──────┬───────────────────────┬───────────────────────┬────────────────────┘  │
│         │                       │                       │                        │
│  ┌──────▼───────┐      ┌────────▼────────┐     ┌────────▼────────┐              │
│  │  任务引擎     │      │  号池/代理/监控   │     │  sub2api 客户端  │              │
│  │  DB 队列      │      │  业务服务层       │     │  (x-api-key)    │              │
│  │  worker 池×20 │      └────────┬────────┘     └────────┬────────┘              │
│  └──────┬───────┘               │                       │                        │
│         │ spawn 子进程           │                       │ HTTPS                  │
│  ┌──────▼──────────────────────▼──────┐        ┌───────▼───────────┐            │
│  │  core/（复用 v1，协议不动）           │        │  sub2api 后端      │            │
│  │  protocol-login.mjs (--json-events)│        └───────────────────┘            │
│  │  sentinel.mjs + sentinel-sdk.js    │                                         │
│  │  tls-transport.mjs                 │  HTTPS                                  │
│  │    └─ spawn python tls_transport.py │──► chatgpt.com / auth.openai.com       │
│  │       (curl_cffi TLS 指纹)          │    sentinel.openai.com                 │
│  │  outlook-mail.mjs / chatgpt-credits│──► Outlook 取件中转 / 8t92.cc           │
│  └────────────────────────────────────┘                                         │
│                                                                                  │
│  SQLite (better-sqlite3) ── data/tosub2.db   日志 data/logs/   断点 data/checkpoints/ │
└──────────────────────────────────────────────────────────────────────────────────┘
```

关键进程模型（与 v1 一致，刻意保留）：

```
Fastify 主进程
 ├── 任务引擎 worker（事件循环内调度，不单独开线程）
 │    └── spawn → node core/protocol-login.mjs --json-events ...   （每任务 1 个子进程）
 │              └── spawn → python core/tls_transport.py           （每任务 1 个 Python worker）
 ├── 代理测活 worker（并发 ≤5 个短生命周期 Python worker）
 └── sub2api 监控定时器（默认 5 分钟一轮）
```

子进程隔离是刻意设计：协议登录崩溃/卡死不影响主服务；Python worker 崩溃只影响单个任务。

## 3. 复用边界（重要）

### 3.1 原样复用（迁移到 `server/core/`，协议逻辑零改动）

| v1 文件 | 行数 | 作用 | v2 位置 |
|---|---|---|---|
| `src/protocol-login.mjs` | 2044 | auth.openai.com 登录 + Codex OAuth + sub2api 导出（CLI） | `server/core/protocol-login.mjs` |
| `src/sentinel.mjs` | 1501 | Sentinel 反检测：requirements PoW + Turnstile DX VM | `server/core/sentinel.mjs` |
| `src/sentinel-sdk.js` | - | 官方 Sentinel SDK 混淆 bundle（vm 沙箱内补丁运行） | `server/core/sentinel-sdk.js` |
| `src/tls-transport.mjs` | 505 | curl_cffi TLS 指纹传输封装（NDJSON 协议） | `server/core/tls-transport.mjs` |
| `src/tls_transport.py` | - | Python curl_cffi worker | `server/core/tls_transport.py` |
| `src/outlook-mail.mjs` | 330 | Outlook 取件 + 余额/封禁关键字识别 | `server/core/outlook-mail.mjs` |
| `src/chatgpt-credits.mjs` | 106 | 余额查询 + refresh_token 轮换 | `server/core/chatgpt-credits.mjs` |
| `src/mail-otp.mjs` | 230 | 泛化邮箱收码 API 轮询 | `server/core/mail-otp.mjs` |
| `src/sms-providers.mjs` 系列 | ~570 | 接码平台适配（Luban/SMSBower/自定义） | `server/core/sms/*.mjs` |

协议细节见 [03-核心协议复用.md](./03-核心协议复用.md)。

### 3.2 允许的最小改造（行为不变）

1. **`protocol-login.mjs` 新增 `--json-events` 模式**（唯一实质改造）：所有人机交互提示输出为 stdout NDJSON 事件流，stdin 改收 JSON 指令。不带该参数时保持 v1 交互 CLI 行为不变。规范见 [03-核心协议复用.md](./03-核心协议复用.md#8-json-events-事件协议规范) 第 8 节。
2. **导出既有纯函数**：v1 该文件无任何 export（纯 CLI）。改造时把 `CookieJar`、`buildSub2apiOauthExport`、`normalizeProxyUrl`、`isRiskControlResponse` 等纯工具函数补上 `export`（函数体不动），供服务端直接引用（测活判定、导入文件解析等）。若不想动该文件，可在 `server/core/risk-control.mjs` 复制 `isRiskControlResponse`（约 10 行）作为共享工具，二选一。
3. **`console-server.mjs` 完全废弃**，其中的业务逻辑（号池流转、sub2api 客户端、任务调度、代理轮换重启）按本文档重写为模块。

### 3.3 完全重写

- 后端 HTTP 服务：`node:http` 手写路由 → Fastify 插件化。
- 前端：单文件 SPA → TypeScript + shadcn-admin 多页。
- 数据层：内存 Map + 零散 JSON → SQLite。
- 凭据存储：DPAPI/Keychain → AES-256-GCM 入库（见 [07-安全设计.md](./07-安全设计.md)）。

## 4. 代码仓库结构

monorepo（npm workspaces），前后端分包：

```
tosub2/
├── package.json                  # workspaces: ["server", "web"]
├── docker-compose.yml
├── Dockerfile
├── requirements.txt              # curl_cffi==0.15.0（Python worker 用）
├── docs/v2/                      # 本文档
├── scripts/
│   ├── migrate-v1.mjs            # v1 → v2 数据迁移（见 09）
│   └── backup.mjs                # SQLite 在线备份
├── data/                         # 运行数据（Docker volume 挂载点）
│   ├── tosub2.db                 # SQLite 主库
│   ├── secret.key                # 应用加密密钥（0600，未提供 env 时自动生成）
│   ├── logs/<jobId>.log          # 任务日志（追加写）
│   ├── checkpoints/<accountId>/  # 登录断点
│   └── results/<jobId>.json      # sub2api 导入文件产物
├── server/                       # 后端
│   ├── package.json
│   ├── index.js                  # 入口：装配 Fastify、启动监听、优雅关闭
│   ├── core/                     # ← v1 复用代码（见 3.1 表）
│   ├── lib/
│   │   ├── db.js                 # better-sqlite3 连接 + 迁移执行器
│   │   ├── crypto.js             # AES-256-GCM 信封加解密、scrypt 口令哈希
│   │   ├── config.js             # 环境变量与启动配置
│   │   ├── logger.js             # pino 实例
│   │   └── http-errors.js        # 统一错误码
│   └── modules/
│       ├── auth/                 # index.js（路由）+ session.js + rate-limit.js
│       ├── proxies/              # index.js + test-worker.js + selector.js
│       ├── accounts/             # index.js + import.js + pools.js + mail-init.js
│       ├── jobs/                 # index.js + engine.js + events.js + launcher.js
│       ├── sub2api/              # index.js + client.js + upload.js + monitor.js
│       ├── settings/             # index.js
│       └── dashboard/            # index.js
└── web/                          # 前端（shadcn-admin 基座）
    ├── package.json
    ├── vite.config.ts
    └── src/
        ├── routes/               # TanStack Router 文件路由（见 06-01）
        ├── components/           # 通用组件（表格、徽章、弹窗…）
        ├── api/                  # fetch 封装 + 类型定义
        ├── stores/               # 极少量客户端状态（主题、侧栏折叠）
        └── lib/
```

## 5. 数据流总览

### 5.1 账号生命周期（主数据流）

```
邮箱.txt 导入
    │  解析四段格式 + 三重查重（备用池内 / 主号池 / 远端 sub2api）
    ▼
备用号池(reserve) ──异步──► 拉邮件初始化：邮箱名 / 初始余额(We've added X credits÷25) / 封禁关键字
    │ 单选或批量「加入主号池」
    │  ① 再拉一次邮件确认未封禁 ② 创建 login 任务(email_otp 模式, 随机存活代理)
    ▼
登录任务(jobs) ──子进程 protocol-login──► OAuth tokens + sub2api 导入文件
    │ 成功: 查实时余额(backend-api/wham/usage) → 移入主号池(main) → 可选自动上传 sub2api
    │ 失败: 回备用池并记录错误
    ▼
主号池(main) ──批量上传──► sub2api（已有→PUT 替换凭据；没有→POST 新增）
    ▲
    │ 监控巡检(5min): sub2api error 账号
    │   ├─ 401/deactivated/banned → 移入废弃池(banned_401)
    │   ├─ 429/rate limit        → 移入废弃池(rate_limited_429)
    │   └─ 其他临时错误          → 自动重登修复(冷却5min) → 修复失败×2 → 移入废弃池
    │   └─ 主池可用数 < 阈值(可选) → 自动从备用池补号
    ▼
废弃号池(discard)（记录原因/时间/原始错误，支持手动移回主池或彻底删除）
```

### 5.2 代理数据流

```
批量导入(每行: 代理URL 或 代理URL----备注)
    → 去重入库(status=unknown)
    → 一键测活(并发5, curl_cffi 过 CF 访问 chatgpt.com) → alive / cf_challenge / dead + 延迟
    → 任何 OpenAI 相关任务启动时: 随机选一条 alive 代理 → 无则本机直连
    → 任务运行中连接失败累计 → fail_count≥3 自动置 dead
```

### 5.3 会话数据流

```
打开页面 → GET /api/v1/auth/session
    ├─ Cookie 有效 → 直接进入（30 天滑动续期）
    └─ 无效 → /login 输密码 → POST /api/v1/auth/login
         → 校验 scrypt 哈希(timing-safe) → 写 sessions 表 → Set-Cookie( HttpOnly/SameSite=Strict )
```

## 6. 技术栈清单

| 层 | 选型 | 版本基线 | 说明 |
|---|---|---|---|
| 运行时 | Node.js | ≥22（ESM） | 与 v1 一致 |
| HTTP 框架 | Fastify | ≥5 | 插件化、JSON Schema 校验、onRequest hook 做认证 |
| 数据库 | SQLite + better-sqlite3 | ≥11 | 同步 API，WAL 模式 |
| 数据校验 | Fastify 内置 JSON Schema（或 TypeBox） | - | 请求体校验 |
| 日志 | pino | ≥9 | 结构化日志，任务日志另落盘 |
| 加密 | node:crypto | - | AES-256-GCM、scrypt、timingSafeEqual |
| 协议核心 | v1 迁移代码 + curl_cffi | curl_cffi==0.15.0 | Python 3.9+ |
| 前端构建 | Vite | ≥6 | |
| 前端框架 | React | ≥19 | |
| 路由 | TanStack Router | - | shadcn-admin 模板自带 |
| 数据获取 | TanStack Query | ≥5 | 轮询策略见 06-03 |
| UI | shadcn/ui + Tailwind CSS | ≥4 | |
| 表格 | TanStack Table | ≥8 | 行选择/排序/分页 |
| 图标 | lucide-react | - | |

## 7. 与 v1 的行为对照（差异清单）

| 行为 | v1 | v2 |
|---|---|---|
| 页面结构 | 单页全塞 | 左侧菜单 9 个页面 |
| 登录令牌 | 进程内存 token，刷新重输 | Cookie 会话 30 天滑动 |
| sub2api 配置 | localStorage + monitor.json 明文 | DB 加密存储，GET 脱敏 |
| 代理 | 单输入框随任务传 | 独立模块，测活 + 随机选路 + 自动降级 |
| 备用号池 | 有（join 后直接上传 sub2api） | 有；加入主池默认不上传，改为主池统一批量上传（可配置加入后自动上传） |
| sub2api 异常账号 | 只自动重登修复 | 分类：401/429 → 废弃池；临时错误 → 修复；可选手动补号 + 自动补号 |
| 任务日志 | 内存 80KB 截断，重启全丢 | 落盘 `data/logs/<jobId>.log`，API 分页读 |
| 任务状态 | 内存 Map + job-meta.json 重建 | jobs 表，重启自动重排队 |
| 凭据 | DPAPI/Keychain（Linux 丢失） | AES-256-GCM 入库，全平台一致 |
| 子进程驱动 | stdout 正则关键字 | NDJSON 事件流 |
| CLI 交互式登录 | 保留 | 保留（`--json-events` 只是新增模式） |
