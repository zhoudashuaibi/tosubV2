# 04-01 后端模块划分

## 1. 原则

- **一个业务域一个 Fastify 插件目录**（`server/modules/<domain>/`），路由、服务、校验 schema 都在目录内，禁止跨域直接 import 别的域的路由文件。
- 跨域共享的能力放 `fastify.decorate`（db、crypto、config、logger、jobsEngine、sub2apiClient 工厂），等价于轻量 DI。
- `server/core/` 是 v1 复用代码，**只被 jobs/sub2api/proxies 三个域引用，且只进不出**（core 不 import modules）。
- 请求体校验用 Fastify 内置 JSON Schema（可换 TypeBox 写法，编译产物相同）。

## 2. 装配顺序（index.js）

```js
import Fastify from 'fastify';
import { openDatabase } from './lib/db.js';
import { initCrypto } from './lib/crypto.js';
import { loadConfig } from './lib/config.js';

const config = loadConfig(process.env);          // 端口/数据目录/密钥来源
const db = openDatabase(config.dataDir);
const crypto = initCrypto(config);               // 加载/生成 secret.key

const app = Fastify({
  logger: pino({ level: config.logLevel }),
  trustProxy: true,                              // 反代场景取真实 IP
  bodyLimit: 2 * 1024 * 1024,                    // 批量导入文本上限 2MB
});

await app.register(async (f) => {                // 装饰器层
  f.decorate('config', config);
  f.decorate('db', db);
  f.decorate('crypto', crypto);
  f.decorate('settings', createSettingsService(db, crypto));
}, { name: 'core-decorators' });

await app.register(authModule);        // 1. auth：注册 onRequest 认证 hook
await app.register(proxiesModule);     // 2. 代理
await app.register(jobsModule);        // 3. 任务引擎（先于 accounts：accounts 依赖引擎提交任务）
await app.register(accountsModule);    // 4. 号池
await app.register(sub2apiModule);     // 5. sub2api（含监控定时器）
await app.register(settingsModule);    // 6. 设置
await app.register(dashboardModule);   // 7. 概览
await app.register(staticModule);      // 8. 前端 dist 静态托管 + SPA fallback（最后注册）

// 启动任务引擎调度循环、恢复中断任务、启动 sub2api 监控（若启用）
await app.jobsEngine.start();
await app.sub2apiMonitor.startIfEnabled();

await app.listen({ port: config.port, host: config.host });
```

**优雅关闭**（SIGINT/SIGTERM）：暂停任务队列调度 → 向所有活跃子进程发 SIGTERM（3s 后 SIGKILL）→ running/awaiting_input 任务置回 queued（attempt 保留）→ 停 sub2api 监控 → `db.close()` → `app.close()`。与 v1 shutdown 顺序语义一致（v1 console-server.mjs:4934-4995）。

## 3. 目录与职责

```
server/
├── index.js                    # 装配 + 生命周期
├── core/                       # v1 复用（见 01-总体架构 §3.1），不改动
├── lib/
│   ├── db.js                   # better-sqlite3 打开 + 迁移执行器 + 通用查询助手
│   ├── crypto.js               # encrypt/decrypt 信封、scryptHash/verify、随机 token
│   ├── config.js               # env 解析：TOSUB2_DATA_DIR/PORT/HOST/SECRET_KEY/CONSOLE_PASSWORD/LOG_LEVEL
│   ├── logger.js               # pino；任务文件日志用单独的 appendFile 助手
│   ├── http-errors.js          # AppError(status, code, message)；全局错误处理格式化
│   └── sanitize.js             # 日志脱敏（token/密码/代理凭证打码）
├── migrations/                 # SQL 迁移（见 02-数据库设计 §6）
└── modules/
    ├── auth/                   # 见 04-02
    │   ├── index.js            # POST /login /logout /logout-all, GET /session, POST /password
    │   ├── session.js          # 会话创建/校验/续期/撤销
    │   └── rate-limit.js       # 登录限流（login_attempts 表）
    ├── proxies/                # 见 04-03
    │   ├── index.js            # CRUD + import + test 路由
    │   ├── test-worker.js      # 短生命周期 curl_cffi 测活（并发 ≤5）
    │   └── selector.js         # pickRandomAliveProxy() + 失败降级记账
    ├── accounts/               # 见 04-04
    │   ├── index.js            # 三池列表/导入/加池/批量操作/废弃/恢复路由
    │   ├── pools.js            # 状态机与池流转事务
    │   ├── import.js           # 四段格式解析 + 三重查重
    │   └── mail-init.js        # 邮件初始化（余额/封禁），调 core/outlook-mail
    ├── jobs/                   # 见 04-06
    │   ├── index.js            # 任务列表/日志/输入/取消/重试路由
    │   ├── engine.js           # 调度循环、并发槽、恢复、取消、代理风控重启
    │   ├── launcher.js         # spawn 子进程 + env 注入
    │   ├── events.js           # json-events 解析 → 状态迁移（纯函数表，便于单测）
    │   └── auto-input.js       # input_required 自动作答（Outlook/收码API/TOTP/接码）
    ├── sub2api/                # 见 04-05
    │   ├── index.js            # config/test/groups/proxies/monitor 路由
    │   ├── client.js           # admin API 封装（x-api-key、120s 超时）
    │   ├── upload.js           # 上传管线（查重→新增/替换→代理分配→余额后缀）
    │   └── monitor.js          # 巡检循环：分类处理 + 自动补号
    ├── settings/
    │   └── index.js            # GET/PUT /settings（Outlook 取件、接码平台、引擎参数）
    ├── dashboard/
    │   └── index.js            # GET /dashboard/summary（聚合查询）
    └── static/
        └── index.js            # @fastify/static 托管 web-dist + setNotFoundHandler SPA fallback
```

## 4. 认证 hook（auth 域提供，全 API 生效）

```js
// modules/auth/index.js 内
f.addHook('onRequest', async (req, reply) => {
  if (!req.url.startsWith('/api/')) return;          // 静态资源放行
  const isPublic = req.routeOptions?.url === '/api/v1/auth/login'
                || req.url.startsWith('/api/v1/health');
  if (isPublic) return;
  const session = await verifySession(req);          // 见 04-02 §3
  if (!session) return reply.code(401).send({ error: { code: 'UNAUTHORIZED', message: '未登录或会话已过期' } });
  req.session = session;
});
```

CSRF 附加校验（写方法）：`Origin` 头必须与 Host 同源 + 请求带 `X-Requested-With: XMLHttpRequest` 头（前端 fetch 封装统一附加）。详见 [07-安全设计.md](../07-安全设计.md)。

## 5. 统一错误格式

```js
// lib/http-errors.js
export class AppError extends Error {
  constructor(status, code, message, extra = {}) { ... }
}
// 全局 setErrorHandler：
reply.status(err.status ?? 500).send({
  error: { code: err.code ?? 'INTERNAL', message: err.expose ? err.message : '服务器内部错误', ...extra }
});
```

错误码清单（全大写下划线，前端按 code 映射文案）：`UNAUTHORIZED / FORBIDDEN / VALIDATION / NOT_FOUND / CONFLICT / RATE_LIMITED / UPSTREAM_ERROR / SUB2API_UNAVAILABLE / JOB_NOT_CANCELABLE / ACCOUNT_STATE_INVALID / POOL_TRANSFER_CONFLICT` 等。业务规则错误用 409/422，依赖 sub2api/邮箱中转的失败用 502。

## 6. settings 服务（lib 级共享）

```js
settings.get(key)                 // 返回解密后的对象（内存缓存）
settings.set(key, value, {secret}) // 加密写库 + 刷新缓存
settings.masked(key)              // 脱敏视图（给 GET 接口用）
```

启动时注入默认值（不存在才写）：`outlook.fetch`、`sms.providers`、`engine.config`、`sub2api.config`（monitor.enabled=false）。
