# 06-01 前端工程结构

## 1. 基座选型

以 [satnaing/shadcn-admin](https://github.com/satnaing/shadcn-admin) 模板为起点（degit 克隆后裁剪）。其技术栈与本项目需求完全对口：

- React 19 + Vite + TypeScript
- **TanStack Router**（文件路由、类型安全）+ **TanStack Query**（数据获取）
- shadcn/ui + Tailwind CSS v4
- 内置：可折叠侧边栏布局、暗色/亮色主题、cmdk 全局搜索、面包屑

裁剪：删除模板的 tasks / profile / 样例 API mock；保留 layout / theme / sidebar / search 骨架。

## 2. 目录结构

```
web/
├── package.json
├── vite.config.ts            # dev 代理 /api → localhost:1999；build 产物 ../server/web-dist
├── components.json           # shadcn/ui 配置
└── src/
    ├── main.tsx
    ├── routes/                       # TanStack Router 文件路由
    │   ├── __root.tsx                # 根：认证守卫 + 全局 Provider（QueryClient/Theme/Toaster）
    │   ├── login.tsx                 # 登录页（无侧边栏布局）
    │   ├── _auth.tsx                 # 认证布局（侧边栏 + 顶栏）
    │   ├── _auth/
    │   │   ├── index.tsx             # /           概览 Dashboard
    │   │   ├── proxies.index.tsx     # /proxies    代理列表
    │   │   ├── pools/
    │   │   │   ├── reserve.index.tsx # /pools/reserve   备用号池
    │   │   │   ├── main.index.tsx    # /pools/main       主号池
    │   │   │   └── discard.index.tsx # /pools/discard    废弃号池
    │   │   ├── jobs.index.tsx        # /jobs       任务中心
    │   │   ├── sub2api.index.tsx     # /sub2api    sub2api 管理
    │   │   └── settings.index.tsx    # /settings   设置
    │   └── routes.ts                 # 路由类型生成（tanstack router 插件自动）
    ├── components/
    │   ├── layout/                   # 模板自带：sidebar / header / theme-toggle / search
    │   ├── data-table/               # TanStack Table 封装（行选择、分页、排序、列显隐）
    │   ├── status-badge.tsx          # 统一状态徽章（任务/账号/代理状态 → 颜色+中文映射）
    │   ├── balance-tag.tsx           # 余额标签（$4.96 / 未查询）
    │   ├── confirm-dialog.tsx        # 危险操作确认
    │   ├── import-dialog.tsx         # 通用批量导入弹窗（textarea + 结果展示）
    │   ├── batch-action-bar.tsx      # 表格选中后浮出的批量操作条
    │   ├── log-viewer.tsx            # 任务日志增量查看器
    │   ├── stat-card.tsx             # 概览统计卡
    │   └── empty-state.tsx
    ├── api/
    │   ├── client.ts                 # fetch 封装（Cookie 同源、X-Requested-With、401 处理、blob 下载）
    │   ├── auth.ts / proxies.ts / accounts.ts / jobs.ts / sub2api.ts / settings.ts / dashboard.ts
    │   └── types.ts                  # 与 05-API接口规范 一一对应的 TS 类型
    ├── hooks/
    │   ├── use-session.ts
    │   ├── use-polling.ts            # useQuery + refetchInterval 封装
    │   └── use-table-selection.ts
    ├── stores/
    │   └── ui.ts                     # zustand：侧栏折叠、主题（模板已有则复用）
    └── lib/
        ├── utils.ts                  # cn() 等（模板自带）
        └── format.ts                 # 时间/金额/持续时长格式化
```

## 3. 侧边栏菜单结构

```
概览          /                LayoutDashboard
号池管理
  ├ 备用号池   /pools/reserve   Inbox
  ├ 主号池     /pools/main      Users
  └ 废弃号池   /pools/discard   Archive
任务中心      /jobs            ListChecks
代理列表      /proxies         Globe
Sub2API      /sub2api         Server
设置          /settings        Settings
```

- 菜单徽章（可选）：备用池「可用 N」、任务中心「进行 N」（来自 dashboard summary，30s 轮询）。
- 暗色/亮色切换放顶栏（模板自带 ThemeToggle）。

## 4. 构建与开发

```jsonc
// web/package.json scripts
{
  "dev": "vite",                      // 开发时 /api 代理到 http://127.0.0.1:1999
  "build": "tsc -b && vite build --outDir ../server/web-dist --emptyOutDir"
}
```

```ts
// vite.config.ts
export default defineConfig({
  server: { proxy: { '/api': 'http://127.0.0.1:1999' } },
  // 允许反代域名访问 dev server（v1 的 allowedHosts: true 场景）
  server: { allowedHosts: true, ... },
})
```

生产模式由 Fastify 托管 `server/web-dist`（SPA fallback 到 index.html），同源无 CORS。

## 5. 认证守卫（__root.tsx）

```tsx
// 伪代码
const { data: session } = useSession()          // GET /api/v1/auth/session
if (session === undefined) return <SplashScreen />
if (!session.authenticated) return <Redirect to="/login" />
if (!session.password_initialized && !isSettingPassword) return <Redirect to="/login?setup=1" />
return <Outlet />
```

`/login` 路由在 `_auth` 布局之外（无侧边栏）。API 层任何 401 → 跳转 `/login`（见 06-03）。

## 6. 国际化策略

模板为英文。本项目**界面文案全部中文硬编码**（用户群为中文），不引入 i18n 框架；仅 ThemeToggle 等模板组件自带英文文案改为中文。组件内文案集中在组件文件顶部常量，便于后续如需 i18n 再抽取。

## 7. 版本与依赖基线

```jsonc
// web/package.json 关键依赖
{
  "react": "^19.0.0", "react-dom": "^19.0.0",
  "@tanstack/react-router": "^1.x", "@tanstack/react-query": "^5.x",
  "@tanstack/react-table": "^8.x",
  "tailwindcss": "^4.x", "lucide-react": "^0.4xx",
  "sonner": "^1.x",                     // toast
  "zustand": "^5.x"                     // 轻量 UI 状态
}
```
